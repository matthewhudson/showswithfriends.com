This is intended to be the starting point for creating a new SG flask app.

You should clone the repository and then customize it from there. A good place to start is to do a `git grep REPLACE` to look for things that are known to need customization.
